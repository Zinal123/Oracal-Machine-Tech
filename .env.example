# URL of the mail server's contact endpoint (see server/README.md).
# During local dev with the server running on its default port:
# VITE_CONTACT_API_URL=http://localhost:3000/api/contact
VITE_CONTACT_API_URL=
